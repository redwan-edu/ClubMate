<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Window colours match the Compose theme, so there is no flash before the first frame. -->
    <style name="Theme.ClubMate" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
